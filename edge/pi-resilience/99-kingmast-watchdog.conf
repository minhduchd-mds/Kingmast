[Manager]
# Requires a supported /dev/watchdog device on the target image.
# The service watchdog catches user-space hangs; RuntimeWatchdogSec covers kernel/systemd stalls.
RuntimeWatchdogSec=20s
RebootWatchdogSec=2min
